// ============================================================
// Debug views — #ifdef DEBUG_* branches (optional, ifdef-guarded)
// ============================================================

// Debug views are conditionally compiled at shader level.
// Specific implementations (DEBUG_RIPPLE, DEBUG_NORMALS, DEBUG_REFLECTION, DEBUG_WAVE_LOD)
// are injected inline in main() where they check specific conditions.
